# sqlrose server configuration
---
connections:
  # A connection's name acts as an ID, i.e. it must be unique within
  # the application scope, no matter how many users, sessions etc.
  - name: sakila
    description: sqlrose sample database, based on MySQL's Sakila sample batabase (https://dev.mysql.com/doc/sakila/en/)
    url:
    username: sqlrose
    password: sqlrose
    # Alternative configuration properties:
    # The dbms protocol could include an optional subsubprotocol...
    #dbms: h2
    #host: localhost
    #port: 1527
    #dbname: sakila
    #props:
    # The property names might be driver-specific...
    #  user: sqlrose
    #  password: sqlrose

users:
